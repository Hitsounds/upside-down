name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build
      run: |
        echo "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\MSBuild\Current\bin\MSBuild.exe  $GITHUB_WORKSPACE\RomanCalc.sln"
        "C:\Program Files (x86)\Microsoft Visual Studio\2019\Enterprise\MSBuild\Current\Bin\MSBuild.exe"  $GITHUB_WORKSPACE\RomanCalc.sln /p:Configuration=Release /p:Platform=x64
    
    - uses: actions/upload-artifact@master
      with:
        name: exe
        path: $GITHUB_WORKSPACE\x64\Release\RomanCalc.exe
