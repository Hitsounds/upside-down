#pragma once
#include <string>

std::string int_to_roman(int integer);
//Does what it says on the tin: takes an int turns it into a roman

int roman_to_int(const std::string& roman);
//Turns romans back into numbers